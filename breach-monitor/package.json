{
  "name": "breach-monitor",
  "version": "1.0.0",
  "description": "Monitor email addresses for dark web data breach exposure via HaveIBeenPwned",
  "type": "module",
  "bin": {
    "breach-monitor": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "tsx src/index.ts",
    "lint": "biome check src tests",
    "lint:fix": "biome check --write src tests",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@vitest/coverage-v8": "^2.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
